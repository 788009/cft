# 可交互蹭饭图

**禁止运行 pnpm**

## 目标

构建一个纯前端、无外部请求的可交互蹭饭图。

应用显示中国地图和同学所在学校，支持地图缩放与移动、行政区详情、稳定的学校标注布局、卡片拖动、实时搜索、个人详情、主题及背景设置、设置与静态资源缓存，以及完整地图和地区详情的 PNG 图片导出。

## 技术栈

- pnpm
- Vite
- Vanilla
- TypeScript
- D3.js，按需导入所需模块
- Tailwind CSS，用于控件、弹窗和响应式界面；地图路径、动态位置及 SVG 状态可使用普通 CSS 或 SVG 属性
- Vitest，用于单元测试
- Playwright，用于交互测试和视觉回归测试

所有依赖、字体、图片、地图和其他资源必须随应用本地提供。禁止使用 CDN、在线字体、在线地图、分析服务或运行时外部 API。

## 数据

所有业务数据均位于 `data/` 文件夹下，只允许读取，不得修改。

Vite 必须将现有 `data/` 作为静态资源提供，并保持其内部目录结构和文件名。不得复制出第二份数据、在源码中内联数据，或为构建过程生成内容相同的数据副本。数据资源的基础路径应集中配置，以兼容 Vite 的开发和生产构建环境。

### `data.csv`

包含以下列：

- `no`：座号
- `name`：姓名，其中第二个字已变为首字母
- `short`：姓名中每一个字均已变为首字母
- `university`：大学名称；约定名称本身包含必要的校区信息，可直接作为学校的唯一分组依据
- `province`：大学所在省份；直辖市与 `city` 相同
- `city`：大学所在城市
- `contact`：联系方式，可能为空
- `lat`：大学纬度；国外学校可能为空
- `lng`：大学经度；国外学校可能为空

姓名转换已经完成，应用不得重新转换或改写姓名。

同一学校内的同学按 `no` 的数值升序显示；相同座号保持 CSV 中的原始顺序。

### 行政区映射

- `province2adcode.json`：数据涉及的国内省份与 adcode 的对应关系
- `cities2adcode.json`：数据涉及的国内城市与 adcode 的对应关系

若一条记录的 `province` 不存在于 `province2adcode.json`，则将其视为国外学校。

直辖市的 `city` 可能不存在于 `cities2adcode.json`。此时使用对应省份的 adcode 作为 city adcode。

### GeoJSON

`geojson/` 包含：

- `china_provinces.json`：全国省级区域
- `10-dash.json`：十段线
- `provinces/`：有学校省份的市级 GeoJSON
- `cities/`：有学校城市的区县级 GeoJSON

`china_provinces.json` 中的 `name`、`adcode` 和 `level` 不一定描述整个省级几何。识别省份必须使用 `province_adcode`，不得依赖该文件中的 `name` 或 `adcode`。

若某行政区域内有学校，应当存在对应层级的 GeoJSON。地图上的“无数据”仅表示该区域没有学校，不表示缺少 GeoJSON。

### 其他数据

- `middle_school_info.json`：中学名称、地区、地址、坐标及可选标题图片。`title_img` 和 `title_img_dark` 均为相对于 `data/` 的路径；暗色图片可省略，图片加载失败时回退到名称文本。
- `teachers.json`：教师职务与姓名。职务值兼容单个字符串或字符串列表，列表中的姓名分行显示。
- `message.html`：设置面板末尾显示的补充内容。前端只保留允许的基础文本元素和 HTTP(S) 链接，忽略脚本、表单、内嵌内容和其他危险元素。

`src/assets/data/province-names.json` 是完整且稳定的省级名称参考表，用于修正省级名称和直辖市在市级视图中的名称。它不随当前业务数据变化，不属于 `data/` 中的业务数据副本。

## 架构约束

严禁让业务逻辑与当前数据内容耦合。不得硬编码学校、姓名、省市、adcode、数量或当前 CSV 中的其他具体值。

至少将以下职责拆分为独立模块：

- 数据加载、校验、标准化与索引
- 地图投影、缩放和行政层级管理
- 视图状态
- 学校标注布局
- 搜索与屏外箭头聚合
- 地区及个人详情弹窗
- 保存图片模式、导出场景与 PNG 编码
- 设置持久化、迁移与静态资源缓存
- 可配置参数及其默认值

学校标注布局应尽量实现为输入明确、结果确定、可单元测试的纯逻辑。交互视图和导出视图可以复用布局规则，但必须分别计算，不能通过截取当前页面代替正式导出。

## 响应式规则

横屏和竖屏均显示完整地图、搜索、设置及详情界面，不再显示横屏使用引导。

信息范围框按可配置的宽屏与窄屏界限切换模式。默认以 `height / width >= 10 / 7` 作为窄屏模式：

- 宽屏模式：默认范围框宽度和高度均为视图对应尺寸的 50%，并居中显示。
- 窄屏模式：默认范围框宽度等于视图宽度，范围框以宽为长边，与视图矩形旋转后相似，并居中显示。

只有跨越宽屏与窄屏界限时才重置为对应模式的默认范围框。同一模式内调整视口尺寸时保留当前范围框的相对位置和尺寸。

搜索控件在大屏显示完整输入框，在宽度小于可配置断点时收起为放大镜按钮。点击后输入框朝视图中心方向展开，再次点击收起。搜索控件会避让同角落的界面控件；固定卡片避让搜索占用区、其他固定卡片和信息卡片。紧凑搜索框展开时允许覆盖地图内容。

主要交互控件的可点击区域不得小于 44 CSS px。

## 主地图

显示可缩放、可移动的中国地图，并显示十段线。

地图按相对于全国初始视图的 D3 缩放倍率 `k` 切换细节层级。阈值必须可配置，默认值为：

- `k < 2.5`：显示省界
- `2.5 <= k < 6`：显示当前涉及区域的市界
- `k >= 6`：显示当前涉及区域的区县界

层级切换必须使用滞后区间，避免在阈值附近反复闪烁。默认进入市级和退出市级的阈值分别为 `2.7` 和 `2.3`；进入区县级和退出区县级的阈值分别为 `6.3` 和 `5.7`。切换时可以使用短暂的透明度过渡。

缺少更细层级 GeoJSON 时，保持当前可用层级，不显示不存在的细节。

无学校的行政区域使用灰色表示，且继续参与正常地图显示和交互边界绘制，但放大后不展示其内部细节。

## 信息显示范围

以视图中心为中心设置一个可配置矩形，只有当前卡片分组对应的地图锚点位于该矩形内时才显示信息。默认形态由宽屏与窄屏模式决定。

信息卡片显示在矩形外、画布内，并通过连线指向对应锚点。卡片支持两种分组方式：

- 按地区分类：默认模式；省级地图按省份聚合，市级和区县级地图按城市聚合，卡片内列出大学和同学。
- 按大学分类：每所大学单独显示卡片，并在大学名称下按座号排列同学姓名。

全部可见信息卡片使用统一缩放比例。全尺寸无法满足边界和避让约束时，按配置步长整体缩小到可用比例。

设置中可显示或隐藏范围框，并进入独立编辑状态拖动或从八个方向调整大小。编辑时可确认或恢复当前宽屏/窄屏模式的默认范围框。

矩形尺寸、画布边距、标签间距以及布局权重必须集中配置，不得散落为魔法数字。

## 稳定标注布局

地图移动和缩放时，同一张信息卡片在可见期间必须连续移动或保持位置，不得瞬间换边、与其他卡片交换位置或重新排列。

布局目标按以下优先级执行：

1. 标签之间不重叠，且不超出可用画布。
2. 已显示学校保持原有位置关系，不瞬移、不换位。
3. 新进入显示范围的学校选择当前代价最低的空位，不推动已有学校重新布局。
4. 避免标签遮挡地图锚点，并尽量减少连线交叉。
5. 在满足以上条件后，尽量缩短连线并使布局紧凑。

卡片锚点离开显示范围时淡出并释放位置。卡片重新进入时，应优先恢复其先前位置。位置不足且确实无法满足硬约束时，允许标签通过 150 至 250 ms 的动画平滑迁移，但不得瞬间跳转。

移动和缩放时不应默认隐藏学校、姓名和连线。仅可将全部隐藏作为明确检测到性能不足时的降级方案。

普通地图、保存图片模式和地区详情中的信息卡片均可拖动，普通学校、中学、国外学校和教师卡片使用相同的手动位置规则。手动位置使用相对于当前画布的归一化坐标，仅在当前页面会话和对应场景中保留，不写入本地设置。

- “持续显示”模式下，地图移动和缩放时手动卡片保持在屏幕固定位置；卡片暂时隐藏后重新显示时恢复原位。
- “操作时隐藏”模式下，地图移动或缩放结束后清除手动位置并重排。
- 调整信息范围框、地图区域比例、卡片分组或布局相关设置时清除手动位置并重排。
- 自动布局不要求避让手动移动后的卡片。

## 地区详情

点击当前地图层级中有学校的行政区域，在弹窗内单独显示该地区地图：

- 当前只显示省界时，点击省份打开省级详情。
- 当前显示市界时，点击城市打开市级详情。
- 区县是最深层级，不再继续下钻。

地区详情地图不可缩放或移动，展示当前地区内的全部学校。详情范围框使用投影后地区几何的外接矩形，并向外增加可配置留白；卡片仍使用主地图的分组、地区名称和布局设置。

弹窗提供“保存图片”按钮。按钮进入与完整地图相同的保存图片模式，但预览和导出仅包含当前弹窗所示地区及该地区全部学校。普通地区详情地图保持不可缩放或移动；进入保存图片模式后的地区地图可以缩放和移动。退出保存图片模式后恢复原地区详情弹窗。

## 个人详情

点击同学姓名后显示个人详情弹窗，包含：

- 大学
- 省份
- 城市
- 联系方式

联系方式为空时隐藏整行，不显示空值或占位符。

## 搜索

搜索支持以下字段：

- `name`
- `short`
- `university`
- `province`
- `city`

比较前统一移除全部空白字符并转为小写。

最终匹配必须严格相等，不要求按下回车，并随输入实时更新：

- 匹配 `name` 或 `short` 时，高亮所有匹配的同学姓名；允许一对多结果。
- 匹配 `university`、`province` 或 `city` 时，高亮所有匹配的学校。

搜索框聚焦时提供联想补全。联想阶段可以使用标准化后的包含匹配；选择联想项或输入形成严格相等时，才产生正式高亮结果。清空输入后清除高亮和屏外箭头。

搜索不得自动移动或缩放地图。

搜索控件所在角落、完整宽度、紧凑模式断点、展开宽度、视口边距和同角落元素间距均由统一配置管理。默认位于左上角；同角落已有设置按钮时，搜索控件排列在其更靠近视图中心的一侧。

## 屏外搜索箭头

若匹配学校的地图锚点位于信息显示矩形之外，则在信息显示矩形边缘显示指向该学校的箭头。

箭头位置由视图中心到学校锚点的射线与信息矩形边缘的交点确定。移动或缩放地图时实时重新计算。

同一条矩形边上相距不超过可配置距离的箭头自动合并，并在合并结果上显示匹配数量；距离增大后自动拆分。默认合并距离为 48 CSS px，配置名为 `searchArrowMergeDistance`。

合并箭头的方向取组内平均方向。箭头的可点击区域不得小于 44 CSS px。

## 国外学校

国外学校没有地图连线，集中显示在可配置的固定角落。位置可选：

- `top-left`
- `top-right`
- `bottom-left`
- `bottom-right`

默认位置为 `bottom-left`，并由统一配置管理。国外学校占用的矩形必须参与国内学校的标注布局约束，国内标签不得与其重叠。

只有当当前视图中所有国内学校的地图锚点均位于信息显示矩形内时，才显示国外学校。国外学校内部同样按大学分组，同一学校内按座号排列姓名。

## 教师卡片

教师信息读取自 `teachers.json`，以固定卡片显示职务及姓名列表。默认位于右上角，角落可配置，并参与信息卡片和其他固定元素的布局避让。

教师卡片只有在当前视图中所有国内学校锚点均位于信息范围框内时显示，与国外学校使用相同的显示条件。

## 中学标记与连线

中学信息读取自 `middle_school_info.json`，默认开启，也可在设置和 `config.ts` 中关闭。关闭中学显示时，同时隐藏中学标记、卡片和红色连接线。

中学使用醒目的红色发光标记，并以红色发光线连接当前位于信息范围框内的大学或地区锚点。连线宽度随对应同学人数增加，地图移动、缩放、范围框或相关设置变化时同步更新；中学锚点位于视图之外时，进入视图的线段仍然显示。

中学卡片仅在中学锚点位于信息范围框内时显示，使用红色边框并参与统一布局。无 `title_img` 时显示名称和地址；有 `title_img` 时按配置尺寸等比显示标题图片。暗色模式优先使用 `title_img_dark`，未提供时继续使用普通图片，图片加载失败时回退到名称。

## 外观与设置

页面标题由 `config.ts` 的 `pageTitle` 设置。主题支持跟随系统、浅色和暗色三种模式。

页面背景支持在 `config.ts` 中分别配置浅色和暗色图片，路径相对于 `data/`。设置面板也可上传本地 PNG、JPEG 或 WebP；上传图片以 `cover` 模式居中显示，同时覆盖浅色和暗色配置，清除后恢复配置图片。文件只在当前浏览器内读取。

设置面板按“外观”“保存图片”“地图信息”“信息卡片”“缓存”分区，包含主题、背景、保存图片入口、地区名称及过滤、范围框显示与编辑、中学显示、卡片分组、交互期间显示方式、可选布局优化、缓存开关和清除缓存。面板最后显示经过安全过滤的 `message.html` 内容。

## 缓存

缓存默认开启，可在设置中关闭或清除。缓存开关保存在独立的 `localStorage` 键中，即使关闭缓存也保留该开关状态；其他设置使用带 `schemaVersion` 的白名单结构保存在 IndexedDB，上传背景图片作为独立 Blob 记录保存。拖动卡片位置、搜索状态、地图缩放和平移、弹窗状态和保存图片过程中的临时状态不得持久化。

设置缓存结构升级必须按版本顺序迁移。新增字段使用当前配置默认值，改名字段显式转换，已删除字段丢弃，枚举或数值无效时回退默认值；不得只通过展开默认对象和缓存对象完成迁移。迁移成功后重写为当前结构，迁移失败时只删除损坏的设置记录并使用默认设置。

静态资源使用构建生成的仅含路径与内容哈希的清单进行校验，不得复制或内联 `data/` 内容。每次页面会话以 `no-store` 请求一次清单：哈希未变的资源直接使用 Cache Storage，不向服务器重复请求；缺失、变更或新增的资源才重新请求；清单中已删除的资源同步移除。Vite 带哈希文件名的构建资源使用 cache-first。清单请求失败时，若存在上一份有效清单和资源缓存则允许离线继续使用，否则回退到普通网络请求。

关闭缓存时停止读写设置缓存，删除设置与上传背景、资源清单元数据和 Cache Storage，并注销当前应用作用域的 Service Worker 后重新加载。清除缓存保留缓存开关，清除上述缓存并将当前界面恢复为默认设置。缓存变更通过 `BroadcastChannel` 通知同源标签页。浏览器自身的 HTTP 缓存不属于应用可完全控制的缓存范围。

## PNG 导出

完整地图通过设置面板中的“保存图片”入口进入保存图片模式；地区详情通过弹窗中的“保存图片”按钮进入同一模式。两者复用 `SaveImageScene` 接口、保存图片菜单和 PNG 编码流程，并分别维护预览状态。

进入保存图片模式后隐藏普通设置按钮和搜索 UI。页面左上角为地图区域，菜单根据视口比例位于右侧或下方，并至少保留对应方向三分之一的空间；剩余空白区域与地图之间必须显示明确边框。菜单与图片尺寸设置共用一个滚动区域，不显示设置面板末尾的致谢内容。

地图区域初始宽高比为 1.6。底边、右边和右下角提供与信息范围框控制点相同视觉样式及交互尺寸的拖动控制点。拖动时实时更新地图、菜单和空白区域，图片目标宽高按地图比例同步变化。修改目标宽度或高度时，另一项按当前地图比例联动。

保存图片模式中的地图支持缩放和移动，禁用行政区点击及地区详情打开。卡片支持拖动，手动位置使用归一化坐标保存；菜单提供“重排”按钮。退出完整地图的保存图片模式后，地图缩放、平移、行政层级和卡片布局恢复首屏状态。

完整地图导出场景包含：

- 完整中国地图
- 十段线
- CSV 中全部国内学校及同学
- 全部国外学校及同学

地区详情导出场景仅包含当前地区地图以及该地区全部学校和同学。地区详情弹窗本身、弹窗边框和操作按钮不进入图片。

导出不得包含搜索框、菜单、按钮或弹窗边框等操作界面，也不继承搜索词或搜索高亮状态。导出必须严格对应当前保存图片预览中的地图缩放和平移、卡片位置、信息范围框、地图比例和样式。

导出行为如下：

- 格式固定为 PNG。
- 默认分辨率为 2880 x 1800。
- 宽度和高度必须是正整数，不设置单边尺寸或总像素数的硬性上限。
- 尺寸低于配置建议值、单边尺寸过大或总像素数过大时，在 `save-image-status` 显示对应原因的警告，但不禁止导出。
- 默认字号与地图区域面积的平方根成正比，比例由 `export.fontScaleAreaRootRatio` 配置，并可在菜单中调整倍率。
- 锚点大小和连线粗细与字号使用相同的面积缩放基准。
- 背景使用当前主题和背景设置，上传图片只在当前浏览器内读取，不得上传或发起外部请求。
- 背景图片适配方式由导出配置决定，当前默认为 `cover`。

导出从保存图片状态创建独立 SVG 场景，按目标尺寸重新计算投影、地图、文字、连线和卡片，不截取页面 DOM。生成前等待字体和本地图片加载，序列化 SVG 后绘制到 Canvas 并编码为 PNG。生成期间保存按钮区域显示进度条和取消按钮；资源加载、SVG 绘制或 Canvas 编码失败时显示明确错误。

## 可配置默认项

以下设置必须由统一配置模块管理：

- 页面标题和数据资源基础路径
- 宽屏信息范围框宽高比例，默认均为 0.5
- 窄屏模式最小高宽比，默认 `10 / 7`，以及窄屏范围框宽度比例，默认 1
- 信息范围框编辑尺寸及交互尺寸
- 地图层级阈值及滞后阈值
- 标签间距、画布边距、布局代价权重及过渡时长
- 卡片统一缩放范围、连线扇出及中学样式
- 屏外箭头合并距离，默认 48 CSS px
- 搜索、设置、国外学校和教师卡片所在角落及搜索响应式尺寸
- 卡片分组、交互模式、地区名称、范围框、中学和布局优化的默认状态
- 主题模式及浅色、暗色页面背景图片
- 缓存默认启用状态
- 导出默认尺寸、初始地图比例、菜单预留比例、字号面积比例、尺寸警告阈值和背景适配方式

## 测试与提交规范

当前协作阶段禁止代理运行 `pnpm`。代理完成改动后应直接结束，由用户运行所需测试并反馈结果；只有用户明确确认测试通过并要求提交后，代理才能提交。每个可独立验收的改动应形成一个独立 git commit，不得把无关改动混入同一提交。

用户在每次提交前至少运行：

```text
pnpm test
pnpm build
```

涉及交互、布局、响应式、地图层级、弹窗、搜索或导出的改动，用户还需运行：

```text
pnpm test:e2e
```

核心单元测试至少覆盖：

- CSV 解析、字段校验及学校聚合
- 座号排序
- 国内外学校判断及直辖市 adcode 回退
- 搜索标准化、严格匹配及一对多结果
- 屏外箭头定位、合并与拆分
- 标注布局不重叠、边界约束及稳定性
- 地图层级阈值和滞后切换
- 导出尺寸校验、尺寸警告、宽高联动及地图区域缩放
- 设置缓存字段白名单、默认值回退及结构版本迁移

Playwright 测试当前覆盖宽屏/窄屏范围框切换、地图缩放移动、地区详情、个人详情、实时搜索、屏外箭头、设置、主题、页面背景、中学、教师、固定卡片、卡片拖动、完整地图保存图片模式和地区详情保存图片模式。视觉变化应在代表性的桌面和移动端尺寸下进行截图验证。

若项目尚未建立对应测试命令，必须先建立测试基础设施；不得以“没有测试”为由跳过后续改动的测试。
