# 可交互蹭饭图

## 目标

一个纯前端应用，可交互的蹭饭图。

## 数据

`data.csv`，包含列
- no: 座号
- name: 姓名，其中第二个字变为首字母
- short: 姓名，每一个字都变为首字母
- university: 大学
- province: 大学所在省份，若是直辖市则 city 相同
- city: 大学所在城市
- contact: 联系方式，可能为空
- lat: 大学纬度
- lng: 大学经度

`data.csv` 已经准备好（包括“变为首字母”也已经处理好），不需要改动，只需要读取。

## 规划

显示一个中国地图，地图可以缩放和移动，只有位于以视图中心为中心的一定大小（可配置）的矩形内部的信息才予以显示，信息显示在该矩形外，以学校为单位，同学的姓名显示在学校底下，学校连一条线指向地图中对应的点，学校、同学、连线要自动以最优方式排列，但是地图缩放和移动时信息不能瞬移（这作为约束之一）。

还要显示一个“下载图片”按钮，可以下载包含一个中国地图以及所有信息的图片。

点击有信息的地区可以单独查看该省份，不是单纯在地图定位并放大，而是将地区地图单独显示，要有过渡动画，此时无法缩放和移动，仅仅展示信息（展示方法同上），并显示一个“下载图片”按钮。

点击空白区域回到上一级。

点击同学姓名，弹窗显示详细信息，包括大学、省份、城市、联系方式（若有），点击空白区域回到上一级。

还要显示一个搜索框，支持搜索姓名（第二个字要变为首字母，对应 name）、首字母缩写（对应 short）、大学、省份、城市，搜索前二者高亮匹配的同学姓名，搜索后三者高亮匹配的学校，均为全字匹配，聚焦在搜索框时，提供联想补全，搜索不需要按下回车，实时匹配。

## 其他

前端无任何外部请求。

技术栈：Vite + Vanilla JS + D3.js + Tailwind CSS

## 规范

1. 严禁与数据本身耦合，其他方面尽量减少耦合
2. 每次改动都要 git commit
3. 每次改动都要测试，测试通过了才能提交
