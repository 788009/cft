# 可交互蹭饭图

## 目标

一个纯前端应用，可交互的蹭饭图。

## 数据

均位于 `data/` 文件夹下。

`data.csv`，包含列
- no: 座号
- name: 姓名，其中第二个字变为首字母
- short: 姓名，每一个字都变为首字母
- university: 大学
- province: 大学所在省份，若是直辖市则 city 相同
- city: 大学所在城市
- contact: 联系方式，可能为空
- lat: 大学纬度
- lng: 大学经度

`data.csv` 已经准备好（包括“变为首字母”也已经处理好），不需要改动，只需要读取。

`province2adcode.json` 和 `cities2adcode.json`，包含 `data.csv` 用到的省市与 adcode 对应。

`geojson/`，包含
- `china_provinces.json`：细分到省。
- `10-dash.json`：十段线。
- `provinces/`：能用到的省的 GeoJSON，细分到市。
- `cities/`，能用到的市的 GeoJSON，细分到区县。

## 规划

显示一个中国地图，地图可以缩放和移动，根据放大程度依次显示省内细节（`provinces/`，若有数据）和城市细节（`cities/`，若有数据），无数据的板块使用灰色表示无数据，放大不展示细节。

只有位于以视图中心为中心的一定大小（可配置，默认长宽各占视图的一半）的矩形内部的信息才予以显示，信息显示在该矩形外，以学校为单位，同学的姓名显示在学校底下，学校连一条线指向地图中对应的点，学校、同学、连线要自动以最优方式排列，但是地图缩放和移动时信息不能瞬移（这作为约束之一）。

还要显示一个“下载图片”按钮，可以下载包含一个中国地图以及所有信息（CSV 中所有国内学校）的图片。

点击有信息的地区可以单独查看该地区（若省还没有放大到显示省内细节，则点击查看该省份，若已经显示省内细节，则点击城市查看城市细节），用一个弹窗将地区地图单独显示，此时无法缩放和移动，仅仅展示信息（展示方法同上），并显示一个“下载图片”按钮，下载的图片包括该省的所有学校。

点击同学姓名，弹窗显示详细信息，包括大学、省份、城市、联系方式（若有，若无则隐藏整行）。

还要显示一个搜索框，支持搜索姓名（第二个字要变为首字母，对应 name）、首字母缩写（对应 short）、大学、省份、城市，搜索前二者高亮匹配的同学姓名，搜索后三者高亮匹配的学校，均为全字匹配，聚焦在搜索框时，提供联想补全，搜索不需要按下回车，实时尝试匹配，匹配不需要移动视图，但若有匹配的结果在允许显示区域之外的，显示向外的箭头。

国外学校在所有国内信息均可以显示时，在固定位置显示，无连线，国外学校的判断标准为，province 字段在 province2adcode 不存在。

地图具体样式等到上述功能完善后再考虑。

## 其他

前端无任何外部请求。

技术栈：pnpm + Vite + Vanilla JS + D3.js + Tailwind CSS

## 规范

1. 严禁与数据本身耦合，其他方面尽量减少耦合
2. 每次改动都要 git commit
3. 每次改动都要测试，测试通过了才能提交
